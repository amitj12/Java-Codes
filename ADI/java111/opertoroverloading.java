import java.util.*;

class maximum
{
	void max(int x,int y)
	{
		if(a>b)
		{
			system.out.println("Maximum no is: "+a;
		}
		else
		{
			system.out.println("Maximum no is: "+b;
		}
	}
	
	void max(float a,float b)
	{
		if(a>b)
		{
			system.out.printl("Maximum no is: "+a;
		}
		else
		{
			system.out.println("Maximum no is: "+b;
		}
	}
}

class demo
{
	public static void main(Stirng at[])
	{
		Scanner sc=new Scanner(System.in)
		system.out.println("Enter two no: ");
			int x,y;
			x=sc.nextInt();
			y=sc.nextInt();
		
		maximum z=new maximum();
			z.max(x,y);

		system.out.println("Enter two float no: ");
		
			m=sc.nextFloat();
			n=sc.nextFloat();

			z.max(m,n);
	}
}
